const input = "let";
let result = [];
for (let i = 0; i < input.length; i++) {
  result.push(input[i]);

  if (input[1] !== input[2]) {
  }
}

console.log(result);
//yet to complete
// Output
["let", "lte", "elt", "etl", "tle", "tel"];
